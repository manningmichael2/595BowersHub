# Dashboard environment variables
# Real values stored in Dashlane

# PostgreSQL (for Anthropic API spend fallback query)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=finance
DB_USER=michael
DB_PASSWORD=CHANGE_ME

# Gmail IMAP (for recent emails tile)
GMAIL_IMAP_USER=manningmichael2@gmail.com
GMAIL_IMAP_PASSWORD=CHANGE_ME_16_CHAR

# Audiobookshelf (for library stats tile)
# Get from: Audiobookshelf UI → User Settings → API Token
AUDIOBOOKSHELF_TOKEN=CHANGE_ME
