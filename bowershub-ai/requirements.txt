# BowersHub AI - Python dependencies
# Pinned to specific versions for reproducible builds.
# Updated May 28, 2026 — latest stable across the stack.

# Web framework
fastapi==0.136.3
uvicorn[standard]==0.48.0

# Database
asyncpg==0.31.0

# AI providers
# Server-side tools (web_search_20250305) need >=0.49 in the Anthropic SDK.
anthropic==0.105.0
boto3==1.43.17

# HTTP client (for webhook calls)
httpx==0.28.1

# Authentication
PyJWT==2.13.0
# bcrypt 5.x kept the hashpw/checkpw API our auth service uses.
bcrypt==5.0.0

# File handling
python-multipart==0.0.29
Pillow==12.2.0
pdfplumber==0.11.9

# Scheduling (hooks)
APScheduler==3.11.2
croniter==6.2.2
# Human-readable cron descriptions for the Scheduled Prompts UI (R11.1).
# Python equivalent of the npm `cronstrue` package referenced in the spec.
# Optional at runtime — services/scheduled_prompts.py falls back to the raw
# cron expression if the import fails.
cron-descriptor==2.0.8

# Push notifications
pywebpush==2.3.0

# Utilities
python-dateutil==2.9.0.post0
